[package]
name = "benchsuite"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
branp-cli = { path = "../.." }
flate2 = "1.0"
tar = "0.4"

[dev-dependencies]
criterion = "0.5"

[lib]
bench = false

[[bench]]
name = "cloc"
harness = false
